# milfo 開発ノート

## プロジェクト概要
- 株価分析・ポートフォリオシミュレーター
- 構成：FastAPI + yfinance（app.py）／バニラJSフロント（index.html）
- ホスト：Render 無料枠

## milfo固有の絶対ルール
1. **yfinance のバージョンを変更する際は、必ず dividendYield の単位
   （小数 or パーセント）を検証スクリプトで確認してから反映する。**
   yfinanceは破壊的変更が頻繁で、過去に dividendYield の単位が
   変わった実績がある。calc_scores と表示整形が単位に依存している
2. **スコア計算ロジック（calc_scores）を複数ファイルに重複させない**

## インフラの制約（Render 無料枠）
- ディスクはエフェメラル。cache.db は再デプロイ・スピンダウンで消える
- SQLiteキャッシュは「負荷軽減用」。staleフォールバックの永続性は
  期待できない（既知の設計上の割り切り）
- 15分アイドルでスピンダウンする。コールドスタート直後はキャッシュが空
- 障害時の第一対処は Manual Redeploy

## データソースの注意点
- yfinance は非公式スクレイピング。Yahoo はデータセンターIP
  （Render含む）に 429 を返しやすい。原因不明の取得失敗時は
  まず Render ログで 429 を疑う
- 日本株は info のフィールド欠損が多い（スコアはフォールバック値50で対応済み）
- 商用化（広告収益化含む）時は yfinance を使えない。移行先の前提：
  - 日本株：J-Quants API（無料プランは12週遅延なので不可、ライト1,650円/月〜）
  - 米国株：Finnhub（無料枠は米国株のみ。国際市場は有償）
- 将来の移行に備え、データ取得は provider 層（fetch_stock 関数）経由に
  集約していく方針。yf.Ticker を直接呼ぶ箇所を増やさないこと

## スコアリング仕様（2026-07-04 実測・修正時点 / yfinance 1.5.1）
- **dividendYield の単位**：yfinance 1.5.1 では既に **%形式**（利回り3% → `3.0`）。
  小数形式（0.03）ではない。無配銘柄は **キーごと欠損**（`info.get()` は None）。
  実測: 8058.T=2.8 / 9432.T=3.7 / VZ=6.65 / T=5.39 / GOOG=0.25 / AMZN・TSLA=キー無し。
  - calc_scores 内で `dy_pct = info.get("dividendYield") or 0`（=%値）に正規化し、
    スコア（`dy_pct / 6.0 * 100`、利回り6%で100点）と API レスポンスの `divYield`
    の**両方**でこの %値を使う。`_fetch_from_yfinance` 側の旧 `*100` は撤去済み
    （単位の一元管理）。**yfinance を更新したら必ずこの単位を再検証すること**
- **欠損時のフォールバック方針**：
  - `div` / `growth` は欠損時 **0点**（フォールバック無し）
  - **50点フォールバックを持つのは value / stable / momentum のみ**
    - value: pbr 欠損 → 50。**per 欠損（赤字含む）→ 25**（バリュエーション不能＝警戒。
      2026-07-04 に旧50から変更。旧仕様は赤字が黒字より高評価になる逆転があった）
    - stable: beta 欠損 → 1.0 として計算（実質50点）
    - momentum: 52週レンジが取れない等で算出不能 → 50
- **しきい値の意味（逆算値）**：
  - 高成長タグ（`growth >= 75`）＝ 成長率 **22.5%以上**
  - 安定タグ（`stable >= 80`）＝ ベータ **0.4以下**
  - momentum は「値上がり率」ではなく **52週レンジ内の現在位置**
    （`(現在値 - 安値) / (高値 - 安値)`）。変化率と混同しないこと
- **growthSource**：revenueGrowth 優先、欠損/0 のとき earningsGrowth で代用。
  どちらを使ったかを API レスポンスの `growthSource`（"revenue"/"earnings"/"none"）
  で返し、フロントは earnings 代用時に成長バー直下へ「利益成長率ベース」と注記する
- **スコープ外（触らない取り決め）**：しきい値の数値、momentum のロジックは
  今回の修正対象外。変更時は上記の逆算値との整合を再確認すること

## 今後の実装予定（優先順）
1. ポートフォリオのJSONエクスポート／インポート（localStorage消失対策）
2. データ提供層（providers.py）の抽象化
3. UptimeRobot等での死活監視（コード外）